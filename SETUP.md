# Setup

Moved to [`INSTALL.md`](./INSTALL.md) — that is the canonical install and
setup guide (requirements, first run, hook configuration, environment
variables, desktop/VS Code/container/MCP setup, database, scripts reference,
and troubleshooting).
