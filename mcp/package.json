{
  "name": "code-agent-monitor-mcp-server",
  "version": "1.0.0",
  "description": "Local MCP server for the Code Agent Monitor",
  "private": true,
  "type": "module",
  "bin": {
    "code-agent-monitor-mcp": "./build/index.js"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('build',{recursive:true,force:true})\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run clean && tsc -p tsconfig.json",
    "start": "node build/index.js",
    "start:http": "node build/index.js --transport=http",
    "start:repl": "node build/index.js --transport=repl",
    "test": "node --import tsx --test __tests__/*.test.ts",
    "dev": "tsx src/index.ts",
    "dev:http": "tsx src/index.ts --transport=http",
    "dev:repl": "tsx src/index.ts --transport=repl",
    "docker:build": "docker build -f Dockerfile -t code-agent-monitor-mcp:local ..",
    "podman:build": "podman build -f Dockerfile -t localhost/code-agent-monitor-mcp:local .."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "code-agent-monitor": "file:..",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.18.0",
    "tsx": "4.23.6",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.22.0"
  }
}
