interface:
  display_name: "Mcp Server"
  short_description: "Configure, launch, validate, and troubleshoot CAM's..."
  default_prompt: "Use $mcp-server to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: false
