interface:
  display_name: "Hook Setup"
  short_description: "Inspect and install CAM monitoring hooks for Claude Code and..."
  default_prompt: "Use $hook-setup to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: false
