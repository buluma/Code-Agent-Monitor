interface:
  display_name: "Health Check"
  short_description: "Run comprehensive health checks on the Code Agent Monitor..."
  default_prompt: "Use $health-check to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: true
