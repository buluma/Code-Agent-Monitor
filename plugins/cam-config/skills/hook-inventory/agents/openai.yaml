interface:
  display_name: "Hook Inventory"
  short_description: "Inventory hooks across the user, project, and project-local..."
  default_prompt: "Use $hook-inventory to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: false
