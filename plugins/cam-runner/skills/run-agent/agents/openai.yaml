interface:
  display_name: "Run Agent"
  short_description: "Launch and supervise Claude Code or Codex through the CAM..."
  default_prompt: "Use $run-agent to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: false
