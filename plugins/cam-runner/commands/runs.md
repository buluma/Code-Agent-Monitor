---
description: List live and persisted CAM-launched Claude Code and Codex runs
---

Run `cam run list` and `cam run history --limit 30`. Summarize live handles
first, then recent persisted runs with provider, model, working directory,
status, session ID, and duration. Do not start or stop anything.
