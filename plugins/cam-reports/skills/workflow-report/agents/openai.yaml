interface:
  display_name: "Workflow Report"
  short_description: "Create a workflow-intelligence report from CAM..."
  default_prompt: "Use $workflow-report to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: true
