interface:
  display_name: "Executive Report"
  short_description: "Generate a concise stakeholder report from CAM sessions,..."
  default_prompt: "Use $executive-report to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: true
