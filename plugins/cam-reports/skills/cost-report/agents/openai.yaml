interface:
  display_name: "Cost Report"
  short_description: "Produce a scoped CAM cost and token report by model,..."
  default_prompt: "Use $cost-report to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: true
