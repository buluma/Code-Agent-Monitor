interface:
  display_name: "Budget Set"
  short_description: "Define a spend budget for Claude Code and, optionally,..."
  default_prompt: "Use $budget-set to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: false
