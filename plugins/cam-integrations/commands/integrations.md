---
description: Read-only inventory of CAM alerts, webhook targets, and remote sources
---

Run `cam alerts --unacked`, `cam alert-rules list`, `cam webhooks`, and
`cam remote-sources`. Produce a compact inventory of open alerts, enabled
rules, webhook health, and remote sync state. Do not mutate or send anything.
