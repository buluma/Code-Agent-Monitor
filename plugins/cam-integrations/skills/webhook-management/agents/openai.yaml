interface:
  display_name: "Webhook Management"
  short_description: "Configure and validate CAM webhook targets across supported..."
  default_prompt: "Use $webhook-management to inspect CAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: false
